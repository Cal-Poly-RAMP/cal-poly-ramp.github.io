---
title: Active Tasks
nav_exclude: true
---

# Task Assignments

## Branch Team

**Members:** Toni, Theodore, Ryan, Jarnail

**Responsibilities:**

-  Design and implement comparator and adder logic to support branch decoding (signed and unsigned operations).
   BLT(U)
   BGT(U)
   BEQ(U)

-  module parameterized
-  comments
-  Ensure that the branch target address is computed correctly (refer to [Mealy's combined-cheatsheet](../_static/pdf/mealy-stuff/233-cheatsheet.pdf)).
-  Refer to the [basic usage guide](https://cal-poly-ramp.github.io/docs/flow-guides/design-and-sim-guide.html) to get familiar with using the asic-tools container
-  Integrate comparator outputs generated by Team A into the existing branch resolver module.
-  Connect the branch resolution logic to the program counter (PC) update path to enable correct redirection during branch instructions.

---

## Muldiv Team

**Members:** Micah, Saul, Robert Bloomfield, Ryan C, Jarnail

**Responsibilities:**

-  Implement a 32×32-bit multiplier module consistent with [RISC-V specifications](https://projectf.io/posts/riscv-multiply-divide/).
-  Account for RISC-V’s `MUL` and `MULH(U/SU)` instructions, where `MUL` gives you the lower 32 bits and `MULH` and it's variants give you the upper 32 bits of the 64-bit product. Remember that $2^{32} * 2^{32} = 2^{64}$.
-  Evaluate two design trade-offs:
-  **Sequential multiplier** – reduced logic requirements, higher cycle latency.
-  **Combinational multiplier** – high logic cost, single-cycle latency.

---

## SPICE Development

**Members:** Jarnail, Ryan, Stanley

**Responsibilities:**

-  Develop MACRO hardening with individual designs
-  Generate a 16kB one-port RAM using SRAM macros
-  Add makefile commands as fit
-  Add example projects to the examples/ folder in carp-tools repository
